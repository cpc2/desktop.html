# Security Notice

desktop.html runs trusted local skins with powerful desktop access. Skins can open files, start programs, run commands, access local paths, and use network resources through the desktop bridge.

Only install skins from sources you trust. The project maintainers are not responsible for third-party skins, user-created skins, or actions performed by skins installed by users.
